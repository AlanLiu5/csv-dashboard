# pandas 数据分析

使用 pandas 实现数据分析。

---

## 功能

- read_csv
- filter
- groupby
- sum / mean / count
- TopK
- summary / insight / chart

---

## 运行

python3 pandas_tool.py --input ../data/data.csv --group-by category --metric sum --field amount --filter city=Melbourne --k 3 --chart

---

## 目标

理解 DataFrame 操作